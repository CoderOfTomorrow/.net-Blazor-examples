@page "/change_bg"

In progress ...

@code {
    private string color = '#' + ColorGenerator.Generator(6);

    private void ChangeRandom()
    {
        color = '#' + ColorGenerator.Generator(6);
    }
}
